import { Router } from 'express'
// TODO: importar controladores de auth
const router = Router()

// router.post('/login', loginController)
// router.post('/register', registerController)
// router.post('/logout', logoutController)

export default router
