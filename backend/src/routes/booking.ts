import { Router } from 'express'
// TODO: importar controladores de booking
const router = Router()

// router.post('/', createBookingController)
// router.get('/', getBookingsController)

export default router
