import { Router } from 'express'
// TODO: importar controladores de servicios
const router = Router()

// router.get('/', getServicesController)
// router.post('/', createServiceController)

export default router
